name: flutter_fire_playground
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_analytics: ^8.0.3
  firebase_auth: ^1.1.4
  firebase_core: ^1.1.0
  firebase_remote_config: ^0.10.0-dev.2
  flutter_hooks: ^0.16.0
  freezed_annotation: ^0.14.1
  hooks_riverpod: ^0.14.0+3
  package_info: ^2.0.0
  simple_logger: ^1.8.0+1
  version: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.2
  freezed: ^0.14.1+3
  json_serializable: ^4.1.1
  pedantic_mono: ^1.11.0+2

flutter:
  uses-material-design: true
  # assets:
  # fonts:
